return {
  "folke/snacks.nvim",
  opts = {
    picker = {
      hidden = true, -- show dot-files
      ignored = true, -- show ignored files
    },

    explorer = {
      hidden = true, -- show dot-files
      ignored = true, -- show ignored files
    },
  },
}
